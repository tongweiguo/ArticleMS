一.遇到的问题:
	1.两个jsp页面不能互相跳转,需要经过一层Controller才能跳转过去,因为被springMVC拦截保护了.
	2.两个Controller也不能互相跳转,需要重定向或者转发,因为springMVC的配置缘故,在返回的url路径前面加了前缀和后缀.
	3.mybatis里面模糊查询,必须这样like '%${name}%'
		写like语句的时候 一般都会写成 like '% %'
		在mybatis里面写就是应该是 like  '%${name} %' 而不是 '%#{name} %'  
		${name} 是不带单引号的，而#{name} 是带单引号的
	4.@Resource annotation is not supported on static fields
		发布的时候会报这个错,不能支持静态字段的注解.但是报错代码里面没有任何静态字段.
		
		
	5.Tomcat增强了其安全性，并且不再允许在查询字符串中使用原始方括号。在请求中，我们有{，} 或其他符号，因此服务器未处理该请求。
		在tomcat conf路径下的server.xml文件里面的 Connector 标签下添加属性 relaxedQueryChars：
		如果应用程序需要默认情况下tomcat不支持的更多特殊字符，请在relaxedQueryChars上述属性中添加这些特殊字符，并以逗号分隔。
二.Springboot注意事项
	1.我们开发 Web 应用的时候，需要引用大量的 js、css、图片等静态资源，在 Spring Boot 中默认提供静态资源访问目录位置需置于classpath下，
目录名需符合如下规则：
			#支持1.5版本，2.0版本
			/static    
			#支持1.5版本，2.0版本
			/public
			#支持1.5版本，2.0版本
			/resources
			#支持1.5版本，2.0版本
			/META-INF/resources
		
		例如：我们可在资源目录 src/main/resources 下创建 static目录，在该位置放置一个图片文件。启动程序后，
		尝试访问http://localhost:8080/xxx.jpg。如能显示图片，配置成功。也可在static目录下建目录,访问时需加上目录即可.




























































































































































愿他待你如初,疼你入骨,从此深情不负.敬你一杯酒,愿你有诗,有梦,有坦荡荡的远方.	

















































































































	